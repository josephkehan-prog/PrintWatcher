"""Companion CLIs bundled into the PrintWatcher distribution.

Each module exposes a `main(argv: list[str] | None = None) -> int` callable
so the unified `printwatcher_app` entry point can dispatch to it.
"""
