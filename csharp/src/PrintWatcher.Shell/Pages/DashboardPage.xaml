<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PrintWatcher.Shell.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:PrintWatcher.Shell.ViewModels"
    xmlns:controls="using:PrintWatcher.Shell.Controls">
    <Page.Resources>
        <Style x:Key="StatTileStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource PanelBrush}" />
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="Padding" Value="20,16" />
            <Setter Property="MinWidth" Value="160" />
            <Setter Property="MinHeight" Value="92" />
        </Style>
        <Style x:Key="StatNumberStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="36" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{ThemeResource TextBrush}" />
        </Style>
        <Style x:Key="StatLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{ThemeResource MutedBrush}" />
            <Setter Property="CharacterSpacing" Value="80" />
        </Style>
    </Page.Resources>

    <ScrollViewer>
        <StackPanel Padding="32,24,32,32" Spacing="20">
            <Grid ColumnSpacing="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="2">
                    <TextBlock
                        Text="PrintWatcher"
                        FontSize="32"
                        FontWeight="Light"
                        Foreground="{ThemeResource TextBrush}" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <controls:StatusPip
                            VerticalAlignment="Center"
                            IsActive="{x:Bind Negate(ViewModel.Paused), Mode=OneWay}" />
                        <TextBlock
                            Text="{x:Bind ViewModel.StatusLabel, Mode=OneWay}"
                            FontSize="13"
                            Foreground="{ThemeResource MutedBrush}"
                            VerticalAlignment="Center" />
                    </StackPanel>
                </StackPanel>
                <Button
                    Grid.Column="1"
                    Content="{x:Bind ViewModel.PauseButtonLabel, Mode=OneWay}"
                    Command="{x:Bind ViewModel.TogglePauseCommand}"
                    MinWidth="100" />
            </Grid>

            <ItemsRepeater ItemsSource="{x:Bind StatTiles}">
                <ItemsRepeater.Layout>
                    <UniformGridLayout
                        MinItemWidth="170"
                        MinItemHeight="92"
                        MinRowSpacing="16"
                        MinColumnSpacing="16" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="vm:StatTileItem">
                        <Border Style="{StaticResource StatTileStyle}">
                            <StackPanel>
                                <TextBlock
                                    Text="{x:Bind Label}"
                                    Style="{StaticResource StatLabelStyle}" />
                                <TextBlock
                                    Text="{x:Bind Value, Mode=OneWay}"
                                    Style="{StaticResource StatNumberStyle}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>

            <controls:OptionsPanel />

            <Border
                Background="{ThemeResource LogBgBrush}"
                CornerRadius="14"
                Padding="16"
                MinHeight="280">
                <StackPanel Spacing="8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Grid.Column="0"
                            Text="ACTIVITY"
                            Style="{StaticResource StatLabelStyle}" />
                        <TextBlock
                            Grid.Column="1"
                            Text="Drop files anywhere on this page to print"
                            Style="{StaticResource StatLabelStyle}" />
                    </Grid>
                    <ListView
                        ItemsSource="{x:Bind ViewModel.Log}"
                        SelectionMode="None"
                        IsItemClickEnabled="False">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Padding" Value="0,2" />
                                <Setter Property="MinHeight" Value="0" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="vm:LogLine">
                                <TextBlock
                                    FontFamily="Consolas, Cascadia Code, monospace"
                                    FontSize="12"
                                    Foreground="{ThemeResource LogTextBrush}"
                                    Text="{x:Bind Line}" />
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
