interface:
  display_name: "PrintWatcher"
  short_description: "Repo-specific patterns and workflows for PrintWatcher"
  default_prompt: "Use the PrintWatcher repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true